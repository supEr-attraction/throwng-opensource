export interface Location {
  lat: number;
  lng: number;
}

export interface Marker {
  itemId: number;
  latitude: number;
  longitude: number;
  albumImage: string;
  songTitle: string;
  artistName: string;
}
